import { Typography } from '@mui/material';

export const Login = (): JSX.Element => {
  return (
    <div>
      <Typography variant="h2" fontSize={'2em'}>
        User Login
      </Typography>
    </div>
  );
};
